/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package nu.handlar.toggle.app;

import nu.handlar.toggle.list.LinkedList;

import static nu.handlar.toggle.utilities.StringUtils.join;
import static nu.handlar.toggle.utilities.StringUtils.split;
import static nu.handlar.toggle.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
